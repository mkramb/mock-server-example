{
  "name": "mock-server",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "set:facts-mock": "ts-node ./scripts/set-facts-mock.ts",
    "set:facts-proxy": "ts-node ./scripts/set-facts-proxy.ts",
    "start": "docker-compose rm -f && docker-compose up --build --force-recreate"
  },
  "dependencies": {
    "@types/node": "^20.11.19",
    "mockserver-client": "^5.15.0",
    "ts-node": "^10.9.2"
  }
}